import sys
from math import *
input = sys.stdin.readline

n = int(input())
for i in range(n):
    a,b = map(int,input().split())
    
    c = factorial(b) // (factorial(a) * factorial(b-a))
    print(c)
